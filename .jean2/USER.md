- On this Windows machine: prefer Herd PHP 8.4 (`C:\Users\PWRbtn1\.config\herd\bin\php84\php.exe`) for PHP/Laravel commands, and use a repaired `cmd.exe /d /c` pnpm PATH/PATHEXT wrapper if plain pnpm fails. Never use Sail automatically unless project explicitly requires it. Ask before DB-changing commands (migrate/refresh/fresh/seed/wipe etc.) and do not run Git state-changing commands without explicit approval; user usually handles Git in PhpStorm.
- During development, do not run `pnpm run build` or other frontend production builds by default; the dev server/build is permanently running. The user will run builds, or the assistant must first explicitly agree/ask before running one.